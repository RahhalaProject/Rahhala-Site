/**
 * Matches backend LookupCategoryEnum.
 * Used when calling getLookupsByCategoryIds API.
 */
export enum LookupCategory {
  RequestType = 1,
  PaymentMethod = 2,
  RentDuration = 3,
  CarType = 4,
  ShipmentType = 5,
  WeightInTon = 6,
  PalletCapacity = 7,
  PrivateCar = 8,
}
